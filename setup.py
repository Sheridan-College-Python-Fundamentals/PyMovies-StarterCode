import setuptools

setuptools.setup(
    name='py-movies',
    version='0.0.1',
    author='Sheridan',
    description='A project to lookup movie information!',
    packages=['py_movies'],
    install_requires=[
        "requests>=2"
    ]
)
